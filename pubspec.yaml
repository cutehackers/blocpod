name: blocpod_workspace
description: Workspace for Blocpod Riverpod event architecture packages.
publish_to: none
version: 0.1.0

environment:
  sdk: ^3.11.5

workspace:
  - packages/arch
  - packages/logger
  - packages/arch_logger

dev_dependencies:
  flutter_lints: ^6.0.0
