name: blocpod_logger
description: Generic log entries, levels, sinks, formatting, and redaction helpers for Blocpod packages.
version: 0.1.1
repository: https://github.com/cutehackers/blocpod
issue_tracker: https://github.com/cutehackers/blocpod/issues
topics:
  - flutter
  - logging
  - observability
  - debugging
  - diagnostics

environment:
  sdk: ^3.11.5

resolution: workspace

dependencies:
  flutter:
    sdk: flutter

dev_dependencies:
  flutter_test:
    sdk: flutter
