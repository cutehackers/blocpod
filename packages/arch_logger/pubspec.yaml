name: blocpod_arch_logger
description: Adapter that maps blocpod_arch event records into blocpod_logger log entries and sinks.
version: 0.1.0
repository: https://github.com/cutehackers/blocpod
issue_tracker: https://github.com/cutehackers/blocpod/issues
topics:
  - flutter
  - riverpod
  - logging
  - observability
  - state-management

environment:
  sdk: ^3.11.5

resolution: workspace

dependencies:
  blocpod_arch: ^0.1.0
  blocpod_logger: ^0.1.0

dev_dependencies:
  flutter_test:
    sdk: flutter
