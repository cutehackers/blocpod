name: blocpod_sample
description: Interactive sample app for Blocpod packages.
publish_to: none
version: 0.1.0

environment:
  sdk: ^3.11.5

resolution: workspace

dependencies:
  blocpod_arch:
    path: ../arch
  blocpod_arch_logger:
    path: ../arch_logger
  blocpod_logger:
    path: ../logger
  flutter:
    sdk: flutter
  flutter_riverpod: ^3.3.1

dev_dependencies:
  flutter_lints: ^6.0.0
  flutter_test:
    sdk: flutter
