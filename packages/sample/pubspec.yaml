name: blocpod_sample
description: Interactive sample app for Blocpod packages.
publish_to: none
version: 0.1.0

environment:
  sdk: ^3.11.5

resolution: workspace

dependencies:
  blocpod_arch: ^0.1.1
  blocpod_arch_logger: ^0.1.3
  blocpod_logger: ^0.1.1
  flutter:
    sdk: flutter
  flutter_riverpod: ^3.3.2

dev_dependencies:
  flutter_lints: ^6.0.0
  flutter_test:
    sdk: flutter
